{
  "name": "cyberpunk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@percy/cli": "^1.27.4",
    "@percy/webdriverio": "^3.0.1",
    "gh-pages": "^6.1.0"
  },
  "dependencies": {
    "vite": "^5.0.5"
  }
}
